# Mako App

A Vite, React, and TypeScript app managed by Mako Apps v2.

## Development

```sh
pnpm install --frozen-lockfile
pnpm dev
```

Use `pnpm build` for a production build and `pnpm typecheck` to validate the
project.
